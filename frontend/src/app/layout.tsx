import type { Metadata } from 'next';
import { Inter } from 'next/font/google';
import './globals.css';
import { Providers } from './providers';

const inter = Inter({ subsets: ['latin'], variable: '--font-inter' });

export const metadata: Metadata = {
  title: 'JobFlow — Distributed Job Scheduler',
  description: 'Production-grade distributed job scheduling platform. Manage queues, workers, and jobs with real-time monitoring.',
  keywords: ['job scheduler', 'queue', 'distributed', 'workers', 'cron'],
};

export default function RootLayout({ children }: { children: React.ReactNode }) {
  return (
    <html lang="en" className="dark">
      <body className={`${inter.variable} font-sans antialiased`}>
        <Providers>{children}</Providers>
      </body>
    </html>
  );
}
